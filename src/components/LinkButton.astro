---
const {
  href,
  class: className,
  target,
  rel,
  ...rest
} = Astro.props;

const isExternalLink = typeof href === 'string' && /^https?:\/\//.test(href);
const finalTarget = target ?? (isExternalLink ? '_blank' : undefined);
const finalRel = rel ?? (finalTarget === '_blank' ? 'noopener noreferrer' : undefined);
---

<a
  {...rest}
  href={href}
  class={`inline-flex min-h-11 items-center gap-x-1 rounded-lg border border-gray-600 bg-gray-900 px-4 py-2 text-sm font-medium text-gray-300 transition hover:bg-gray-700 hover:text-white focus:z-10 focus:outline-none focus:ring-4 focus:ring-gray-700 ${className ?? ''}`}
  target={finalTarget}
  rel={finalRel}
>
  <slot />
</a>
